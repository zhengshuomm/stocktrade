name: 持仓量异常检测

on:
  schedule:
    # 美国西部时间 (PST/PDT) 周一到周五
    # 冬季时间 (PST = UTC-8): 6:00 AM PST = 14:00 UTC, 7:00 AM PST = 15:00 UTC
    # 夏季时间 (PDT = UTC-7): 6:00 AM PDT = 13:00 UTC, 7:00 AM PDT = 14:00 UTC
    # 使用两个 cron 表达式覆盖冬夏两季
    - cron: '0 13,14 * * 1-5'  # 夏季时间 (PDT)
    - cron: '0 14,15 * * 1-5'  # 冬季时间 (PST)
  workflow_dispatch:  # 允许手动触发

jobs:
  detect-oi-outliers:
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      
    - name: 设置 Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install yfinance pandas numpy xlsxwriter
        
    - name: 运行持仓量异常检测
      run: |
        echo "开始持仓量异常检测..."
        cd program
        python find_outliers_by_oi.py
        echo "持仓量异常检测完成"
        
    - name: 提交异常检测结果
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add outlier/
        if git diff --staged --quiet; then
          echo "没有新的异常检测结果需要提交"
        else
          git commit -m "Auto-update: 持仓量异常检测结果 $(date '+%Y-%m-%d %H:%M')"
          git push
        fi
